<?php

/*namespace scr\migrations;

require '../config/bootstrap.php';

use Illuminate\Database\Capsule\Manager as Capsule;

if(!Capsule::schema()->hasTable('issues')){
    Capsule::schema()->create('issues', function ($table) {
        $table->increments('id');       
        $table->string('description');
        $table->boolval('todo')->default(false);
        $table->boolval('doing')->default(false);
        $table->boolval('done')->default(false);
        $table->timestamps();       
    });
}*/
